# testing a custom workflow

name: Suricate Bank CI
on: [push]

jobs:

  setup:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/setup-go@v2
          with:
            go-version: 1.17
            
  lint:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - run: make metalint

  test:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - run: go test -v ./...
  
  build:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - run: go build -v ./...
